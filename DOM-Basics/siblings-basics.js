// siblings ate thode nodes that are childrens of the same parent node.

Element.siblings; //gives all the siblings of the element in an array
Element.nextsibling; //gives the next sibling of the element
Element.previoussibling; //gives the previous sibling of the element
Element.parentsibling; //gives the parent sibling of the element
Element.hassiblings(); //returns true if the element has siblings else returns false
