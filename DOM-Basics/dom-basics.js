//DOM tree refers to the html page where all the nodes are objects and it can be of three main types of nodes in DOM tree :- 
/* 1. Text node
   2. Element node
   3. Comment node
*/

document.head; //gives the head element of the document
document.body; //gives the body element of the document
document.documentElement; //gives the root element of the document (html element)