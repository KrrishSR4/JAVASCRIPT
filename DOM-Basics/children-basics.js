// children node:- elements that are direct children for eg head and body are childrens of a <html> element

Element.firstchild; //gives the first child element of the parent element
Element.lastchild; //gives the last child element of the parent element
Element.childnodes; //gives all the child elements of the parent element in a node list
Element.haschildnodes(); //returns true if the element has child nodes else returns false