//arrays can be looped through by using the classical javascript for loop or through some other methods.

let a = [1, 7, 9]

for (let i of a) {
    console.log(i)//loggs the values of the array in very next lines
}

