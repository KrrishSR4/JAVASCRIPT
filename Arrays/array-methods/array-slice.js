// slice() it slices out a piece from an array. It creates a new array


let a = [1, 7, 9, 8, 5];
// uses slice()

console.log(a.slice(0, 3))//loggs the sliced part of the array *here slicing starts from index 3 to the end*