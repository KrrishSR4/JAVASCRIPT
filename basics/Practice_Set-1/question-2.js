//use type of operator to find the data type of the string in the above question
let a = "Krrish"
let b = 4  

console.log (typeof (a+b))

//output will be string because when we add a number to a string it concatenates the number to the string and the result is a string