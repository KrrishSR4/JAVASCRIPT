// setInterval method has a similar syntas as setTimeout

// Unlike setTimeout, it runs the function not only once, but regularly after the given intreval of time

setInterval(function() {
    console.log("krish")
}, 4000) //logs krish every 4 seconds

